function spiralMatrix(rows, cols) {
    let currentRow = ''

    for (let row = 0; row < rows; row++) {
        for (let i = 1; i <= cols; i++) {
            currentRow += i + ' '
        }
    }
}

//Unfinieshed
